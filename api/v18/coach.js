const crypto=require('crypto');
const {json,cors,verifyUser,getState,countAI,compactStoredContext,openAI,serviceFetch,errorResponse}=require('../../server/v18-lib');
module.exports=async(req,res)=>{if(cors(req,res))return;try{if(req.method!=='POST')return json(res,405,{ok:false,error:'Method not allowed'});const user=await verifyUser(req);let {message,mode='coach',imageDataUrl=null,threadId=null,context=null}=req.body||{};message=String(message||'').trim();if(!message&&!imageDataUrl)return json(res,400,{ok:false,error:'Ask a question or add a photo.'});if(message.length>8000)return json(res,413,{ok:false,error:'Question is too long.'});if(imageDataUrl&&(!/^data:image\/(?:png|jpe?g|webp);base64,/i.test(imageDataUrl)||imageDataUrl.length>8_000_000))return json(res,413,{ok:false,error:'Use a JPG, PNG or WebP image under about 6 MB.'});await countAI(user.id);if(!context){let row=await getState(user.id);context=compactStoredContext(row?.state)}let instructions=`You are Work + Workout Coach, an evidence-informed fitness, schedule and nutrition assistant inside a consumer app. Personalize answers using the supplied user context. Be concise, practical and specific. Never invent logged data. Training advice must respect work shifts, commute, sleep, recovery, limitations and stated equipment. Nutrition advice must respect the user's goal, restrictions and familiar foods while fitting calorie, protein and fiber targets. Do not diagnose disease or present estimates as medical facts. If a user reports an injury, severe symptom, eating-disorder behavior, pregnancy, or a medical condition requiring individualized treatment, recommend appropriate professional guidance. For equipment photos, identify the equipment only when reasonably confident, explain adjustment/setup, safe start position, movement path, common errors, and which exercise-library names match. If uncertain, say what visible feature would distinguish it. Do not identify people in images. Context JSON follows:\n${JSON.stringify(context).slice(0,24000)}`;if(mode==='equipment')instructions+='\nThe current task is equipment identification and safe usage guidance from an image. Focus on the equipment, not any person visible.';const out=await openAI({instructions,text:message||'Identify this gym equipment and explain how to use it safely.',imageDataUrl,model:process.env.OPENAI_COACH_MODEL||'gpt-5.6-terra'});const tid=threadId&&/^[0-9a-f-]{36}$/i.test(threadId)?threadId:null;try{const id=tid||crypto.randomUUID();await serviceFetch('chat_messages',{method:'POST',body:[{user_id:user.id,thread_id:id,role:'user',message:message||'[equipment photo]',mode},{user_id:user.id,thread_id:id,role:'assistant',message:out.text,mode}],prefer:'return=minimal'});threadId=id}catch{}return json(res,200,{ok:true,answer:out.text,threadId})}catch(e){errorResponse(res,e)}};
