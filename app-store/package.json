{
  "name": "work-gym-coach-ios",
  "private": true,
  "version": "1.0.0",
  "description": "Native iOS packaging project for Work + Gym Coach",
  "type": "module",
  "scripts": {
    "icon": "bash scripts/make-icon.sh",
    "build:web": "node scripts/build-web.mjs",
    "ios:add": "npm run icon && npm run build:web && npx cap add ios",
    "ios:sync": "npm run icon && npm run build:web && npx cap sync ios",
    "ios:open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.5.0",
    "@capacitor/ios": "^8.5.0",
    "html5-qrcode": "^2.3.8",
    "tesseract.js": "5.1.1",
    "tesseract.js-core": "5.1.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
