// Nutrition settings --------------------------------------------------------
function fillNutritionForm(){let n=nutrition(),map={setGymCal:'gymCal',setRestCal:'restCal',setProtein:'protein',setFat:'fat',setFiber:'fiber',setSodium:'sodium',setSatFat:'satFat',setWater:'water',setGoalWeight:'goalWeight',setGoalBf:'goalBf',setPlanStart:'planStart'};for(const [id,k] of Object.entries(map))$('#'+id).value=n[k];$('#nutritionApplyBox').classList.add('hidden')}
function saveNutritionForm(){let n={gymCal:+$('#setGymCal').value||2300,restCal:+$('#setRestCal').value||2100,protein:+$('#setProtein').value||180,fat:+$('#setFat').value||70,fiber:+$('#setFiber').value||30,sodium:+$('#setSodium').value||2300,satFat:+$('#setSatFat').value||20,water:+$('#setWater').value||96,goalWeight:+$('#setGoalWeight').value||0,goalBf:+$('#setGoalBf').value||0,planStart:$('#setPlanStart').value||dkey()};saveNutritionObj(n);closeModal('nutritionDialog');renderAll();toast('Nutrition goals saved')}
function applyCalorieRecommendation(){let r=bodyRecommendation();if(!r.delta)return;let n=nutrition();n.gymCal=Math.max(1200,+n.gymCal+r.delta);n.restCal=Math.max(1200,+n.restCal+r.delta);saveNutritionObj(n);fillNutritionForm();renderAll();toast(`${r.delta>0?'+':''}${r.delta} calories applied`)}
